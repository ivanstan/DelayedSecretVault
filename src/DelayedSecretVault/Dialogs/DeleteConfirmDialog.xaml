<Window x:Class="DelayedSecretVault.Dialogs.DeleteConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete secret"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="{StaticResource UiFont}">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#E7EEF2" Offset="0"/>
            <GradientStop Color="#F4F8F7" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <StackPanel Margin="24">
        <DockPanel Margin="0,0,0,14">
            <TextBlock DockPanel.Dock="Left"
                       Text="&#xE74D;"
                       FontFamily="{StaticResource IconFont}"
                       FontSize="20"
                       Foreground="{StaticResource DangerBrush}"
                       Margin="0,0,10,0"
                       VerticalAlignment="Center"/>
            <TextBlock x:Name="PromptText"
                       FontSize="16"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>
        </DockPanel>
        <TextBlock Text="Type the secret name to confirm:" Margin="0,0,0,4" Foreground="{StaticResource MutedBrush}"/>
        <TextBox x:Name="ConfirmBox" Margin="0,0,0,8"/>
        <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="100" Margin="0,0,8,0" IsCancel="True" Click="Cancel_Click"/>
            <Button Content="Delete" Style="{StaticResource DangerButton}" Width="100" Padding="14,9" IsDefault="True" Click="Delete_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
