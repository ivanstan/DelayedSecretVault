<Window x:Class="DelayedSecretVault.Dialogs.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="{StaticResource UiFont}">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#E7EEF2" Offset="0"/>
            <GradientStop Color="#F4F8F7" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <StackPanel Margin="24">
        <DockPanel Margin="0,0,0,18">
            <TextBlock DockPanel.Dock="Left"
                       Text="&#xE713;"
                       FontFamily="{StaticResource IconFont}"
                       FontSize="20"
                       Foreground="{StaticResource AccentBrush}"
                       Margin="0,0,10,0"
                       VerticalAlignment="Center"/>
            <TextBlock Text="Settings"
                       FontSize="20"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <TextBlock x:Name="AccessDelayLabel" Margin="0,0,0,4" Foreground="{StaticResource MutedBrush}"/>
        <TextBox x:Name="AccessDelayBox" Margin="0,0,0,8"/>
        <TextBlock x:Name="AccessDelayHint"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource MutedBrush}"
                   Margin="0,0,0,14"/>

        <TextBlock Text="Secret reveal duration (seconds)" Margin="0,0,0,4" Foreground="{StaticResource MutedBrush}"/>
        <TextBox x:Name="RevealDurationBox" Margin="0,0,0,14"/>

        <TextBlock Text="Clipboard clear duration (seconds)" Margin="0,0,0,4" Foreground="{StaticResource MutedBrush}"/>
        <TextBox x:Name="ClipboardDurationBox" Margin="0,0,0,8"/>

        <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="100" Margin="0,0,8,0" IsCancel="True" Click="Cancel_Click"/>
            <Button Content="Save" Style="{StaticResource PrimaryButton}" Width="100" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
